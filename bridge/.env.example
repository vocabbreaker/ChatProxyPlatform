# Bridge UI Configuration (Optional)
# 
# This file is for reference only when running with Docker.
# Docker setup uses docker-compose.yml environment variables.
#
# For Windows Docker deployment:
# - Edit docker-compose.yml to change FLOWISE_PROXY_URL
# - Or set FLOWISE_PROXY_URL environment variable before running start.bat
#
# Examples:
# - Local development: http://host.docker.internal:8000
# - Production: https://your-api-domain.com
#
# Default (if not set): http://host.docker.internal:8000

# Example configuration (not used by Docker):
# VITE_FLOWISE_PROXY_API_URL=https://your-api-domain.com
